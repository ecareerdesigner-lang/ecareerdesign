export const metadata = {
  title: "Free ATS Resume Checker | eCareer Design",
  description: "Check your resume's ATS score for free in 30 seconds. Get your Overall Score, ATS Score, Keyword Score, and Formatting Score — no account required.",
};

import ATSCheckerTool from "./ATSCheckerTool";

export default function ATSCheckerPage() {
  return <ATSCheckerTool />;
}
